# The Eclipse

横スクロール型 2D アクション RPG（DxLib / C++17 / Visual Studio 2022 / 1920x1080）。

「自由に動いて斬る」リアルタイムアクションと、モーションが固定される
**ソードスキル** を組み合わせたハイブリッド戦闘のプロトタイプです。

```
タイトル → ホーム（装備 / クエスト / 強化 / 設定）→ クエスト（2〜4 フロア＋ボス）→ リザルト → ホーム
```

---

## 1. ビルド方法

### 必要なもの

| 項目 | 内容 |
|------|------|
| IDE | Visual Studio 2022（C++ によるデスクトップ開発） |
| ライブラリ | [DxLib](https://dxlib.xsrv.jp/)（バージョン指定なし / VC 用パッケージ） |
| 言語 | C++17 |

### 手順

1. DxLib の VC 用パッケージを入手し、`プロジェクトに追加すべきファイル_VC用` フォルダを
   任意の場所に置きます（`DxLib.h` や `DxLib.lib` が入っているフォルダです）。
2. 下のどちらかで場所を指定します。
   - **環境変数**: `DXLIB_DIR` にそのフォルダのパスを設定する（推奨）
   - **配置**: リポジトリ直下に `external\DxLib\` として置く
3. `TheEclipse.sln` を Visual Studio 2022 で開く。
4. 構成を `Debug` または `Release`、プラットフォームを `x64`（または `x86`）にしてビルド。
5. `bin\<プラットフォーム>\<構成>\TheEclipse.exe` が生成され、`assets` が自動でコピーされます。

> パスの解決は `TheEclipse\DxLib.props` で行っています。別の配置にしたい場合はここを編集してください。
> リンクエラーが出る場合は、ライブラリディレクトリ（`$(DxLibLibDir)`）が正しいかを確認してください。
> DxLib は **静的 CRT（/MT・/MTd）** と **マルチバイト文字セット** を要求するため、
> プロジェクト側で既にその設定を行っています。
>
> `LNK2019` 等の未解決シンボルが出る場合は、DxLib.h の自動リンク（`#pragma comment`）が
> 効いていない可能性があります。プロジェクトのプロパティ →
> リンカー → 入力 → 追加の依存ファイル に、お使いの DxLib パッケージ同梱の
> `.lib` 一式（`DxLib.lib` など）を追加してください。

### 文字コードについて

ソースは **UTF-8（BOM なし）** で記述し、コンパイラオプション `/utf-8` と
`SetUseCharCodeFormat(DX_CHARCODEFORMAT_UTF8)` を対で使用しています。
日本語が文字化けする場合はこの 2 つが有効かを確認してください。

---

## 2. 操作方法

| 操作 | キー / マウス |
|------|--------------|
| 移動 | `A` / `D`、`←` / `→` |
| **奥・手前へ移動** | `W` / `S`、`↑` / `↓` |
| ジャンプ | `SPACE` |
| 通常攻撃（3 段コンボ） | 左クリック / `J` |
| ガード | 右クリック / `K`（長押し・被ダメージ約 72% カット） |
| **パリィ** | **ガード中に左クリック**（敵の攻撃に合わせる） |
| 回避ダッシュ | `SHIFT`（無敵時間あり） |
| ソードスキル | `1` `2` `3` `4` / 右下アイコンをクリック |
| メニュー（戦闘中） | `ESC`（ゲームに戻る / 装備 / 設定 / リタイア） |
| 決定 / キャンセル | `ENTER` / `ESC` |

ホームでは `1`〜`4` キーでも下部タブを開けます。UI はすべてマウスで操作できます。

---

## 3. 実装済みの内容

### 奥行き（ベルトスクロール）

`W` / `S` で画面の奥・手前へ移動できます。2D の横スクロールのまま、
Z 軸方向にも動ける『ファイナルファイト』型のフィールドです。

- 奥に行くほど**小さく・暗く**描画され、手前のキャラクターが前面に重なります。
- **奥行きが合っていないと攻撃は当たりません**（許容幅は約 58）。
  敵もプレイヤーの奥行きに合わせて回り込んできます。
- ボスの咆哮のように、奥行き全域を巻き込む攻撃もあります。
- 床は奥行きの帯として描画され、歩ける範囲がひと目で分かります。

### 戦闘

- **通常攻撃**: 3 段コンボ。段ごとに倍率・ノックバック・踏み込みが異なり、先行入力に対応。
- **ソードスキル**: 発動中はモーション固定。定義された時刻に多段ヒットが発生し、
  技によって無敵時間・突進・打ち上げが付く。**クールダウン制**（MP も消費）。
- **パリィ**: ガード中に攻撃ボタンを押すと 0.18 秒の受付が発生し、
  その間に正面から攻撃を受けると成立。ダメージ 0・MP 回復・無敵に加え、
  **相手をよろけさせて反撃の隙**を作る。飛び道具は威力 1.8 倍で**弾き返す**。
  失敗すると 0.5 秒間パリィできない硬直が発生（連打は不可）。
- 武器種ごとに専用スキル **6 種（計 30 種）**。**スキルツリーで解放し、4 つまで装備**できます。
  武器を変えるとロードアウトは自動で組み直されます。
- ヒットストップ、画面振動、クリティカル、コンボカウンタ、ガード、被弾無敵。

### 武器種（5 種）

片手剣 / 片手棍 / 短剣 / 細剣 / 槍。射程・攻撃速度・クリティカル傾向がそれぞれ異なり、
プレースホルダー描画の見た目（武器形状）も変わります。

### 装備・ドロップ

- スロット: **武器（右手）/ 武器（左手）/ 頭 / 体 / 盾 / 腕 / 手 / 足**（8 種）
  - 武器は**右手・左手のどちらにも**装備できますが、通常は**片手だけ**です
    （片方に装備すると、もう片方の欄には「武器（右手）に装備中」のように表示されます）。
  - ユニークスキル「二刀流」を習得すると**両手に片手剣**を装備できます。
    両手に武器を持つと**盾は装備できません**。
  - 左手だけに武器を持った場合は、能力値はそのまま全部加算されます
    （減衰するのは両手持ちの左手だけ）。
- 入手はガチャではなく **ボスドロップ＋道中ドロップ** のみ。
- ステータス: 攻撃力・防御力・最大 HP / MP・クリティカル率・クリティカル倍率・
  移動速度・攻撃速度・MP 回復。
- **強化（+1 ごとに +9%）**: col と強化結晶を消費。+3 までは必ず成功、以降は成功率が低下
  （失敗しても強化値は下がりません）。売却も可能。
- **耐久力**: 武器は攻撃を当てるたび、防具は被弾するたびに消耗します。
  **クエスト中でも 0 になった時点でその場で消滅**します（画面中央に警告を表示）。
  鍛冶屋で修理でき、傷んだ装備は売値も下がります。

#### 個体値

レアリティ（N / R / SR …）は廃止し、装備 1 つ 1 つが持つ **個体値（0〜100）** に置き換えました。
同じ装備でも個体値が違えば性能が変わります。

| 個体値から決まるもの | 0 のとき | 100 のとき |
|---------------------|---------|-----------|
| 能力値の倍率 | **0.60 倍** | **1.80 倍** |
| 強化上限 | **+4** | **+14** |
| 最大耐久力 | **100** | **180**（強化 +1 ごとにさらに +4） |
| 強化費用 | 1.00 倍 | 3.20 倍 |
| 修理費用（耐久力 1 点あたり） | 8 col | 24 col |
| 売値 | 1.00 倍 | 5.80 倍 |

- **同じ個体値なら必ず同じ性能**になります（生成時のランダムなブレは無くしました）。
- 一覧では数値バッジと帯の長さ・色（灰 → 水色 → 金）で高さが分かります。
- ドロップの個体値はクエストごとに範囲が決まっており、
  **難易度が高いほど上振れしやすく**なります（同じ範囲から複数回引いて最大値を採用）。
  クエスト選択画面のボスドロップ欄に、その範囲を表示しています。
- 数値の調整は `src/Game/Equipment.h` の `IvStatScale` / `IvMaxUpgrade` /
  `IvDurabilityBonus` にまとめてあります。

### 鍛冶屋

ホームの「鍛冶屋」タブで、装備の**強化**と**耐久力の修理**を行います。

- 修理費用は減った耐久力 × 個体値係数（個体値 0 は 8 col/点、100 は 24 col/点）。
- 「すべて修理」で所持品をまとめて修理できます。
- 装備の耐久力が 25% 以下になるとホーム画面と鍛冶屋タブに警告が出ます。
- 耐久力はクエスト中にリアルタイムで減り、**0 になった装備はその場で失われます**。
  壊れた瞬間に警告が出るので、`ESC` →「装備」から替えの装備に持ち替えられます
  （壊れた分はリザルト画面でも一覧表示されます）。

### ユニークスキル

通常のスキルとは別枠の、特別な能力です。**複数ある場合でも習得できるのは 1 つだけ**で、
**解放条件を満たすまでスキルメニューには表示されません**。

| ユニークスキル | 効果 | 解放条件 |
|---------------|------|---------|
| 二刀流 | 片手剣を両手に装備できる（盾は装備不可） | 「蝕の祭壇」を **50 秒以内**で解放可能 |

- **どれか 1 つでも解放条件を満たした時点で、他のユニークスキルは二度と手に入りません。**
  例えば「蝕の祭壇」を 50 秒以内でクリアすると、その瞬間に二刀流で確定します。
  - 締め切られた後は、他の解放条件を満たしてもリザルト画面には何も表示しません
    （**解放できたものだけ**を知らせます）。
  - デバッグモードでは締め切りを無視して全て解放できます。
- 解放条件を満たすと、スキル画面にそのユニークスキル名のタブ（例:「二刀流」）が現れます。
  タブ名は習得しているユニークスキルによって変わります。
- **専用ツリーの先頭スキル（ダブル・サーキュラー）を「解放する」と、その時点で習得**します。
- 専用スキルは **ダブル・サーキュラー → スターバースト・ストリーム → ジ・イクリプス**
  の 1 列 3 段。習得している間だけ解放・装備できます。

#### ユニークスキルは独立した系統

ユニークスキルは、片手剣・片手棍などの**武器種と並ぶ独立した系統**として扱います。
どちらの系統を使うかは**装備**で決まります。

| 装備 | 使える系統 | スキル枠 |
|------|-----------|---------|
| 両手に片手剣（二刀流が働いている） | 二刀流の専用スキルのみ | 3 |
| 片手だけに片手剣 | 通常の片手剣スキルのみ | 4 |
| 片手棍・短剣・細剣・槍 | その武器種のスキルのみ | 4 |

- 両手持ちの間は**片手剣のスキルは使えません**。逆に片手持ちに戻すと
  **専用スキルは使えず**、通常の片手剣スキルへ自動で戻ります。
- そのため「装備中」バッジも、両手持ちの間は**武器種のタブではなくユニークのタブ**に付きます。
- 系統が切り替わると、装備していたスキルは自動で入れ替わります。
  **クエスト中に武器を変えた場合もその場でスロットが組み直され**、
  新しい武器用のスキルがセットされます。
- 左手の武器はステータスへ **60%** で加算されます（`kOffHandStatRate`）。
- 解放条件の数値は `src/Game/UniqueSkill.h` の先頭にまとめてあり、
  `kDualWieldClearTimeLimit`（秒）と `kDualWieldQuestId` を書き換えるだけで変更できます。
- デバッグモード中は戦闘画面の `F6`、またはホーム左下の「ユニーク解放」ボタンで解放できます。

### スキルツリー

- 各武器種に **2 系統 × 3 段** のツリー。系統は「火力特化」と「機動・範囲」。
- 下の段は上の段が前提。解放には **スキルポイント（SP）** を消費（1 → 2 → 3 SP）。
- SP は **レベルアップ 1 回につき 1**、クエスト初回クリアで **+2**。
- 各武器の起点スキルは最初から解放済み。初期 SP は 3。
- **装備できるのは 4 つまで**。ホームの「スキル」タブから装備します。
  未使用 SP があるとタブにバッジが出ます。

### クエスト

| # | クエスト | フロア数 | ボス |
|---|---------|---------|------|
| 1 | はじまりの森 | 3 | 深緑の狼王 ファングルフ |
| 2 | 石牢の回廊 | 4 | 石牢の守護者 ゴーレム・ガルド |
| 3 | 蝕の祭壇 | 4 | 蝕の騎士 エクリプス・ナイト |

- 各フロアの敵を全滅させるとゲートが開き、右端から次のフロアへ。最終フロアがボス戦。
- ボスは **HP 70% / 35% でフェーズ変化**（攻撃力・速度上昇、予備動作短縮）。
  薙ぎ払い・突進・叩きつけ＋衝撃波・遠距離弾・連続斬り・咆哮をパターン選択で使い分けます。
- 終了時に **リザルト**（ランク評価 / 戦闘記録 / 報酬 / ドロップ一覧）を表示してホームへ戻ります。

### UI

- 左上にキャラアイコン＋HP / MP バー、上部中央にボス HP、右下にスキルバー（クールダウン表示）。
- ホーム下部に **装備 / スキル / クエスト / 鍛冶屋 / 設定** の 5 タブ（`1`〜`5` キーでも開閉）。
- 戦闘中の画面上にメニューボタンは置かず、`ESC` のみで開きます。メニューの内容は
  **ゲームに戻る / 装備 / スキル / 設定 / クエストリタイア**。
  - **装備**: 壊れた装備をその場で交換できます
    （能力値と見た目が即座に反映されます。戦闘中の売却はできません）。
  - **スキル**: クエスト中でもスキルの解放・付け替えができます
    （変更はその場で戦闘に反映されます）。
- 設定画面は 2 カラム構成（左: 音量・表示・開発者向け／右: 操作一覧）。
- 日本語フォント対応（`assets/fonts` に配置したフォントを自動読み込み）。

### プレイヤー名

- **初回起動時（セーブデータが無いとき）だけ**、タイトル画面で名前入力ウィンドウが開きます。
  決めるまでゲームを始められません（キャンセル不可）。
- 入力は**画面上の文字パネルをクリック**する方式です。IME に依存しないため、
  フルスクリーンでも環境差なく日本語を入力できます。
  - **ひらがな / カタカナ / 英数字** の 3 タブ
  - `゛゜`ボタンで直前の文字の濁点・半濁点を切り替え（`は → ば → ぱ → は`）
  - `小文字`ボタンで直前の文字を小書きに（`つ → っ`）
  - `1 文字削除` / `全消去`、`BACKSPACE` でも 1 文字消せます
- 最大 **12 文字**。文字数は UTF-8 の文字単位で数えるので、途中で切れることはありません。
- **変更は開発者モード中のみ**。設定画面の「開発者向け」にある
  「プレイヤー名を変更」から開きます（デバッグモードが OFF の間は押せません）。

### セーブ / ロード

- `save/save.txt` に**平文**で保存します（内容を確認・編集しやすい形式）。
- **自動セーブ**: ホームに戻ったとき / 出撃前 / ゲーム終了時。
- 起動時にセーブデータがあれば自動で読み込みます。
- 設定画面の「セーブデータ削除」から消去できます（2 回押しで確定）。
  削除するとそれ以降は**自動セーブを止める**ので、終了時に書き戻されず、次回起動は最初からになります。
- 設定画面の「ログアウト」でセーブしてゲームを終了できます（2 回押しで確定）。
- 保存内容: **プレイヤー名**・レベル・経験値・所持品（個体値・強化値・耐久力を含む）・
  装備（左右の武器）・col・強化結晶・解放スキル・スキルポイント・スキル構成・
  ユニークスキル・クリア状況・各種設定。
- 装備を戻してからスキル構成を復元します（使えるスキルの系統は装備で決まるため）。
- v4 以前のセーブデータも読み込めます。旧レアリティは個体値へ読み替えます
  （N → 10 / R → 30 / SR → 50 / SSR → 70 / UR → 90）。

### マウスカーソル

- 起動時と画面モード切り替え時に `SetMouseDispFlag(TRUE)` を呼び、
  フルスクリーンでもカーソルが表示されるようにしています。
- それでも見えない環境向けに、設定の「カーソルを描画」で
  **ソフトウェアカーソル**（ゲーム側で矢印を描画）へ切り替えられます。
  ON のときは OS のカーソルを隠すので二重表示にはなりません。

### デバッグモード（開発者向け）

設定画面の一番下のトグルで有効化します。戦闘中に以下が使えます。

| キー | 機能 |
|------|------|
| `F1` | 無敵の切り替え |
| `F2` | フロアの敵を殲滅（ボス含む） |
| `F3` | HP / MP 全回復 |
| `F4` | 当たり判定の表示 |
| `F5` | col +10000 / 強化結晶 +50 / SP +5 |
| `F6` | ユニークスキルを全て解放（締め切りを無視。習得の切り替えも可能） |

有効時は左側に座標・奥行き・状態・判定数などの情報パネルを表示します。

ホーム画面では左下にボタンが並びます。

| ボタン | 機能 |
|--------|------|
| `col +10,000` | col を追加 |
| `素材 +50` | 強化結晶を追加 |
| `SP +10` | スキルポイントを追加 |
| `全武器取得` | 全武器種の武器を個体値 70 で入手（片手剣は二刀流を試せるよう 2 本ずつ） |
| `スキル全習得` | 全武器のスキルツリーを解放（習得中のユニークの専用スキルを含む） |
| `ユニーク解放` | ユニークスキルを解放して習得 |

### 素材

スプライトシートと PNG 連番の **両方** に対応。未配置の場合は図形ベースの
プレースホルダー（武器種・敵種別ごとに形状が変化）で動作します。
配置方法は [`assets/README.md`](assets/README.md) を参照してください。

---

## 4. ディレクトリ構成

```
The-Eclipse/
├── TheEclipse.sln
├── TheEclipse/
│   ├── TheEclipse.vcxproj / .filters
│   ├── DxLib.props              DxLib の場所を解決
│   └── src/
│       ├── main.cpp             エントリポイント（WinMain）
│       ├── Common/              ベクトル・矩形・色・数学・文字列
│       ├── Core/                DxLib 初期化 / 入力 / フォント / リソース / カメラ / シーン管理
│       ├── Graphics/            描画ユーティリティ / アニメーション / キャラ描画
│       ├── Game/                ステータス・装備・スキル・戦闘・敵・ボス・ステージ・クエスト
│       ├── UI/                  HUD・各種パネル・ウィジェット
│       └── Scenes/              Title / Home / Quest / Result
└── assets/                      画像・フォント（詳細は assets/README.md）
```

ヘッダ（`.h`）と実装（`.cpp`）は全て分離しています。

---

## 5. 主要な調整ポイント

| 調整したい内容 | ファイル |
|--------------|---------|
| 画面サイズ・重力・ジャンプ力・配色 | `src/Core/GameConfig.h` / `.cpp` |
| 通常攻撃のコンボ・ダッシュ性能 | `src/Game/Player.cpp` 冒頭の `kCombo` など |
| パリィの受付時間・硬直・報酬 | `src/Game/Player.cpp` 冒頭の `kParry*` |
| パリィ時のよろけ時間 | `src/Scenes/QuestScene.cpp` 冒頭の `kParryStagger*` |
| ソードスキルの追加・調整 | `src/Game/SwordSkill.cpp` |
| スキルツリーの構成・解放コスト | `src/Game/SwordSkill.cpp` の `kTree` |
| スキルポイントの入手量 | `src/Game/PlayerData.cpp` / `src/Scenes/QuestScene.cpp` |
| 装備の追加・ステータス | `src/Game/ItemDatabase.cpp` |
| 強化コスト・成功率・修理費用 | `src/Game/Inventory.cpp` |
| 耐久力の最大値 | `src/Game/Equipment.cpp` の `MaxDurability` |
| 耐久力の消耗ペース | `src/Scenes/QuestScene.cpp` 冒頭の `kWeaponWearPerHit` など |
| 設定画面のレイアウト | `src/UI/SettingsPanel.cpp` の `Layout()` |
| 敵の性能 | `src/Game/EnemyDatabase.cpp` |
| ボスの性能・使用パターン | `src/Game/Boss.cpp` |
| クエスト・フロア地形・ドロップ | `src/Game/QuestDatabase.cpp` |
| ダメージ計算式 | `src/Game/Stats.cpp` の `CalculateDamage` |
| 奥行きの広さ・当たり判定の許容幅 | `src/Core/GameConfig.h` の `kDefaultFieldDepth` / `kHitDepthRange` |
| ユニークスキルの解放条件 | `src/Game/UniqueSkill.h` の `kDualWieldClearTimeLimit` など |
| 左手武器のステータス寄与率 | `src/Game/Inventory.cpp` の `kOffHandStatRate` |
| セーブ形式 | `src/Game/SaveData.cpp` |

---

## 6. 今後の拡張予定（未実装）

- マルチプレイとパーティ編成・スイッチ攻撃
  （`Actor` を基底に複数キャラを扱える構造にしてあります）
- BGM / SE の再生（音量設定の項目のみ用意済み）
- パッシブスキル（現在のツリーはアクティブスキルのみ）
